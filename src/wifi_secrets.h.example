#ifndef WIFI_SECRETS_H
#define WIFI_SECRETS_H

// Copy this file to wifi_secrets.h and fill in your real values there.
// wifi_secrets.h is gitignored — it will never get committed.
// If left blank, the firmware skips WiFi entirely and the radar still
// works standalone (screen mirror just won't be available).

#define WIFI_SSID     ""
#define WIFI_PASSWORD ""

#endif
